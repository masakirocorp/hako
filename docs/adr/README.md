# Architecture Decision Records

ADRs record current architectural decisions that future maintainers and agents should preserve unless a later ADR supersedes them. Linear tracks ADR workflow; this directory is the source of truth for ADR content.

## When to add an ADR

Add an ADR only when the decision is hard to reverse, surprising without context, and the result of a real trade-off. Prefer a Linear `kind:adr` issue for tracking the work; the ADR file remains the source of truth.

Use the stricter Gardn bar for new ADRs: the decision should cross module or workflow boundaries, still be active, not be obvious from code alone, and be likely to be simplified incorrectly by a future maintainer or agent if the rationale is not recorded. Skip ordinary helper design, local implementation details, broad test coverage notes, and behavior already implied by an existing ADR.

## Index


- [0001 — Keep live terminal runtimes outside AppState](0001-separate-state-from-runtime.md): Gardn stores workspace structure and terminal metadata in state, while live terminal runtimes stay behind `TerminalRuntimeRegistry`.
- [0002 — Keep AppState rendering read-only](0002-keep-rendering-pure.md): `compute_view*` reconciles view/layout state, and `render*` draws from the computed `AppState` without mutating app, workspace, or layout state.
- [0003 — Isolate reusable platform behavior behind platform APIs](0003-isolate-platform-behavior.md): reusable macOS/Linux process and host-integration behavior belongs behind `apps/gardn/src/platform/` APIs instead of being duplicated at call sites.
- [0004 — Decouple fallback screen detection from terminal viewport state](0004-decouple-detection-from-viewport.md): fallback agent detection classifies recent bottom-of-buffer text while runtime handles sampling, process identification, stabilization, and hook arbitration.
- [0005 — Split app orchestration by responsibility](0005-split-app-orchestration-by-responsibility.md): app behavior is placed across state, actions, input, runtime, and focused helpers instead of accumulating in one god object.
- [0006 — Version the wire protocol by release](0006-version-wire-protocol-by-release.md): server/client compatibility is tracked with an explicit protocol version reviewed against tagged Gardn releases.
- [0007 — Isolate multi-agent work in task worktrees](0007-isolate-multi-agent-work-in-task-worktrees.md): Worktrunk-backed task worktrees isolate bigger, risky, parallel, or dirty-shared-checkout work so the shared checkout remains safe for integration.
- [0008 — Treat upstream as signal in the product fork](0008-treat-upstream-as-signal.md): changes from upstream are candidate input that Gardn ports by invariant while preserving Gardn product identity.
- [0009 — Separate session snapshot, history, and handoff state](0009-separate-session-snapshot-history-and-handoff.md): durable layout/launch state, optional scrollback history, and handoff-only terminal semantics stay on separate persistence paths.
- [0010 — Keep client input byte-framed and server-decoded](0010-keep-client-input-byte-framed.md): thin clients frame input bytes while the server owns normal app input semantics and routing.
- [0011 — Make direct terminal attach exclusive by default](0011-make-direct-terminal-attach-exclusive.md): one direct attach client owns writable access to a terminal unless another client explicitly takes over.
- [0012 — Use GitHub Releases for direct updates](0012-use-github-releases-for-direct-updates.md): direct installs update from fixed GitHub Release binary assets, while package-managed installs defer to their manager.
- [0013 — Split local API from client wire protocol](0013-split-local-api-from-client-wire-protocol.md): the JSON local API is Gardn's control plane, while the separate binary wire protocol carries interactive client rendering and input.
- [0014 — Vendor libghostty-vt as terminal core](0014-vendor-libghostty-vt-as-terminal-core.md): Gardn builds a pinned vendored libghostty-vt source tree and keeps Gardn-owned Rust glue around the terminal-emulation boundary.
- [0015 — Gate live handoff by server capability](0015-gate-live-handoff-by-server-capability.md): update-time live handoff is opt-in and used only when the running server advertises support; otherwise Gardn falls back to explicit stop/keep behavior.
- [0016 — Define agent profile catalog and integration authority](0016-define-agent-profile-catalog-and-integration-authority.md): agent launch profiles are global layered catalog entries, while integration reports separately govern state, session identity, and metadata authority.
- [0017 — Treat config.toml as a public contract](0017-treat-config-toml-as-public-contract.md): `config.toml` is a stable hand-editable API, settings writes rewrite narrow sections, and live reload applies valid sections with diagnostics instead of all-or-nothing replacement.
- [0018 — Separate workspace groups from workspace identity](0018-separate-workspace-groups-from-workspace-identity.md): groups organize and filter workspaces, workspaces own tabs/panes/runtime identity, and public IDs stay separate from raw runtime IDs.
- [0019 — Scope project command discovery and reuse managed runs](0019-scope-project-command-discovery-and-reuse-managed-runs.md): project commands are discovered from workspace pane roots and reruns focus or restart a managed command pane instead of spawning duplicates.
- [0020 — Treat port registry as observed runtime state](0020-treat-port-registry-as-observed-runtime-state.md): port entries are host TCP listener observations attached to likely pane process-tree owners, not configured resources or workspace identity.
- [0021 — Centralize notification policy before delivery](0021-centralize-notification-policy-before-delivery.md): agent state changes become app-level notification intent before Gardn chooses in-app, terminal, system, sound, or no delivery.
- [0023 — Stage clipboard-image paste through thin clients](0023-stage-clipboard-image-paste-through-thin-clients.md): clipboard-image paste is read by the pasting thin client, staged as a temporary server file, and delivered to terminals as a file path.
- [0024 — Merge terminal metadata by source and expiry](0024-merge-terminal-metadata-by-source-and-expiry.md): integration metadata reports merge by source, field, guard, sequence, and TTL before affecting effective pane presentation.
- [0025 — Keep local API events bounded and replayable](0025-keep-local-api-events-bounded-and-replayable.md): Local API events are process-local recent history with internal sequence cursors and bounded replay, not durable audit logs.
- [0026 — Preserve compatibility while translating terminal keys](0026-preserve-compatibility-while-translating-terminal-keys.md): input dialects normalize into semantic terminal keys while output keeps legacy/xterm encodings where they are more compatible.
- [0027 — Cache Kitty graphics by host view state](0027-cache-kitty-graphics-by-host-view-state.md): host-terminal image uploads and placements are cached separately, while view and scrollback changes re-display placements to keep graphics synchronized with text repainting.
- [0028 — Keep one foreground client authoritative for host context](0028-make-one-foreground-client-authoritative-for-shared-runtime-state.md): one foreground full app client supplies global host focus, theme, app-facing keybindings, and notification context; per-tab PTY sizing and interactive input authority follow ADR 0056.
- [0029 — Negotiate per-client render encoding and prefer fresh frames](0029-negotiate-per-client-render-encoding-and-prefer-fresh-frames.md): each client negotiates its render stream encoding and keeps its own baseline, while droppable render delivery favors the latest frame over replaying stale frames.
- [0030 — Perform live handoff through a dedicated import socket and FD exchange](0030-perform-live-handoff-through-dedicated-import-socket-and-fd-exchange.md): live handoff uses a private token-validated import socket, manifest exchange, fd transfer, and explicit commit/rollback ownership protocol.
- [0031 — Plan native agent resume from trusted session refs and launch context](0031-treat-native-agent-resume-identity-as-launch-context-plus-session-ref.md): native agent restore plans combine trusted session refs with preserved launch command/env context, while dedupe uses source, agent, session ref, and validated saved environment.
- [0032 — Normalize settings interactions through a shared row model](0032-normalize-settings-interactions-through-shared-row-model.md): settings sections render and handle input from typed rows so scroll, selection, mouse hit-testing, and section-specific content stay aligned.
- [0033 — Inherit workspace context when creating workspaces and tabs](0033-inherit-workspace-context-when-creating-workspaces-and-tabs.md): interactive workspace creation inherits source workspace group/cwd context; new tabs and agent-profile tabs inherit cwd from their workspace, and workspace auto-naming is group-local.
- [0034 — Let terminal runtimes own process teardown unless handoff preserves them](0034-let-terminal-runtimes-own-process-teardown-unless-handoff-preserves-them.md): live terminal runtimes own PTY/session-process shutdown by default, while committed live handoff explicitly preserves old runtimes until the replacement assumes ownership.
- [0035 — Route runtime surfaces through the session namespace](0035-route-runtime-surfaces-through-session-namespace.md): active session names re-root Gardn's data directory, sockets, logs, handoff import socket, and status reporting, while explicit session requests scope update targeting.
- [0036 — Clear persistence for factory-default session state](0036-clear-persistence-for-factory-default-session-state.md): Gardn deletes normal session/history files when app state returns to the built-in empty/default shape instead of writing an explicit empty snapshot.
- [0037 — Write persistence through user symlinks](0037-write-persistence-through-user-symlinks.md): session persistence resolves existing, relative, and dangling symlinks before writing so user-managed dotfile topologies are preserved.
- [0038 — Cap protocol payloads and shed oversized visuals](0038-cap-protocol-payloads-and-shed-oversized-visuals.md): wire and thin-client transport payloads are capped at defined stages, and oversized visual render data can be dropped or retried text-only rather than blocking interactivity.
- [0039 — Normalize pane terminal identity at spawn](0039-normalize-pane-terminal-identity-at-spawn.md): pane spawns advertise Gardn's stable inner terminal identity by default instead of inheriting the outer host terminal's `TERM`/`COLORTERM`.
- [0040 — Scope PTY output compatibility rewrites](0040-scope-pty-output-compatibility-rewrites.md): Gardn permits narrowly identified PTY byte rewrites, such as Droid primary-screen scrollback-clear filtering, while preserving normal terminal output elsewhere.
- [0041 — Restore host terminal theme after transient overrides](0041-restore-host-terminal-theme-after-transient-overrides.md): pane-level OSC tracking restores cached host colors after transient child default-color changes once the shell regains the primary-screen foreground.
- [0042 — Synthesize command palette from live app state](0042-synthesize-command-palette-from-live-app-state.md): command palette entries combine fixed actions with active workspace tabs, visible workspaces, groups, active-workspace agent availability, and custom commands before filtering and section sorting.
- [0043 — Model group default agent profiles as favorites](0043-model-group-default-agent-profiles-as-favorites.md): group defaults stay visible by being launchable favorites, and New Agent resolves them from the target workspace's group before opening the picker.
- [0044 — Share modal layout and hit-test primitives](0044-share-modal-layout-and-hit-test-primitives.md): modal overlays reuse stack, list viewport, and tab-strip geometry so rendering, scrolling, and mouse hit-testing stay aligned across surfaces.
- [0045 — Enforce testing policy with maintenance guardrails](0045-enforce-testing-policy-with-maintenance-guardrails.md): narrow maintenance tests mechanically reject high-signal weak test patterns while leaving subjective behavioral quality to review.
- [0046 — Keep Nix optional and build-only](0046-keep-nix-optional-and-build-only.md): Nix provides flake packages, apps, checks, dev shells, and overlays for Nix users without becoming Gardn's primary test, update, or release channel.
- [0047 — Restore invalid or legacy snapshots into stable workspace shapes](0047-restore-invalid-or-legacy-snapshots-into-stable-workspace-shapes.md): restore migrates legacy snapshots, recovers missing pane cwd paths, and preserves metadata-only workspace shells when workspace identity remains valid.
- [0048 — Layer agent state evidence by source precedence](0048-layer-agent-state-evidence-by-source-precedence.md): hook state reports, process/profile detection, terminal screen signals, and seen/unseen state compose by evidence strength instead of any single source winning unconditionally.
- [0049 — Test agent integrations against current CLI releases](0049-test-agent-integrations-against-current-cli-releases.md): optional real-agent smoke tests pin image tooling but install current coding-agent CLIs by default so compatibility probes match users who update frequently.
- [0050 — Supersede native Git diff sessions](0050-embed-native-git-diff-sessions.md): Gardn opens a configured external Git diff command in a managed tab instead of owning native file/hunk diff UI or selected-diff agent payloads.
- [0051 — Add Gardn plugin v1](0051-add-gardn-plugin-v1.md): Gardn plugins are global unsandboxed local extensions with Gardn-native manifest, environment, and API names, no earlier-name aliases, and session-local runtime attribution.
- [0052 — Split shared session state from client view state](0052-split-shared-session-state-from-client-view-state.md): one Gardn server owns shared workspace/runtime state, while each normal app client owns independent navigation, transient UI state, computed geometry, and scroll/selection view state; Tab Control assigns explicit per-tab interactive authority.
- [0053 — Adopt Gardn product identity](0053-adopt-gardn-product-identity.md): Gardn uses `gardn` as its canonical machine namespace, owns `masakirocorp/gardn` and `gardn.dev`, requires wire protocol 13 for the clean identity cutover, and leaves the snapshot version unchanged because old namespace files are not read.
- [0054 — Keep the public website and documentation static](0054-keep-public-website-and-documentation-static.md): the public site remains one asset-only deployment, generated reference remains disposable, and a dynamic product app waits for real server-authoritative state; ADR 0055 supersedes its framework selection.
- [0055 — Use FumaPress for the public website](0055-use-fumapress-for-public-website.md): one static FumaPress package owns custom marketing and Fumadocs-backed documentation routes, with Oxc checks and Cloudflare Workers Static Assets deployment.
- [0056 — Control shared terminal tabs explicitly](0056-control-shared-terminal-tabs-explicitly.md): one controller per stable tab identity owns canonical PTY size and interactive input; watchers are view-only until explicit takeover, with no auto-promotion.
- [0057 — Coordinate mixed-host execution from one Session Namespace](0057-coordinate-mixed-host-execution.md): one coordinator owns Shared Session State and host-qualified placements while Local/SSH execution adapters own host runtime operations; standalone remote coordinators remain a separate role with isolated state and runtime ownership.
- [0058 — Isolate the Local API contract in a leaf crate](0058-isolate-local-api-contract.md): pure Local API DTOs, schema generation, and compatibility fixtures live in an acyclic workspace crate so runtime and UI edits can reuse their compiled macro expansions.
- [0059 — Keep stable and beta as official release lines](0059-keep-stable-and-beta-release-lines.md): official Direct Installs follow either GitHub latest or `vX.Y.Z-beta.N` prereleases, share `~/.config/gardn`, and refuse mixed-binary attach.
- [0060. Own ghui as a separately released companion fork, superseded](0060-own-ghui-as-a-companion-fork.md). GitHub is now a built-in client-local ratatui screen using existing `gh` authentication. The companion decision remains as history, with Kit Langton's MIT attribution retained for adapted behavior.
